import org.scalatest.Spec
import org.scalatest.GivenWhenThen

class Twitter4JTwitterClientSpec extends Spec with GivenWhenThen {
	
	describe("The client") {
		it("Should return an empty list when there are no mentions") (pending)
		it("Should return mentions since the specified Tweet Id") (pending)
	}
}